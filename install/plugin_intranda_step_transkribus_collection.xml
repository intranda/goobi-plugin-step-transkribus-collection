<config_plugin>
    <!--
        order of configuration is:
          1.) project name and step name matches
          2.) step name matches and project is *
          3.) project name matches and step name is *
          4.) project name and step name are *
	-->
    
    <config>
        <!-- which projects to use for (can be more then one, otherwise use *) -->
        <project>*</project>
        <step>*</step>
        
        <!-- Login name for Transkribus user -->
        <transkribusLogin>User</transkribusLogin>
        
        <!-- Password for Transkribus user -->
        <transkribusPassword>Password</transkribusPassword>
        
        <!-- URL for the Transkribus API to use -->
        <transkribusApiUrl>test</transkribusApiUrl>
        
        <!-- Name of the Collection where the documents shall be added to -->
        <transkribusCollection>test</transkribusCollection>
       
        <!-- Base URL of the viewer where the public available METS files are located -->
        <viewerBaseUrl>test</viewerBaseUrl>

        <!-- Identifier to use for the viewer Base URL -->
        <identifierField>$(meta.CatalogIDDigital)</identifierField>
        
    </config>

</config_plugin>
